import { UrlI } from "../interface/UrlI";

// URL:er, COURSES är URL:en där kurserna ska hämtas ifrån
export const URL: Readonly<UrlI> = {
  COURSES: 'https://webbutveckling.miun.se/files/ramschema.json'
}